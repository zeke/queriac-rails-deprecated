
%h2 Commands

%ul
  - @commands.each do |command|
    %li= link_to user_command_path(command.user, command), user_command_path(command.user, command)

- if logged_in?
  = link_to("New Command", new_user_command_path(current_user))

%h2 Users

%ul
  - @users.each do |user|
    %li= link_to user_path(user)
