%h2 Edit Command

= semantic_form_for [current_user, @command] do |f|

  = f.inputs do

    #editor

    = f.input :name
    = f.input :keyword
    = f.input :script
    = f.input :description
  
  = f.actions do
    = f.action :submit

= include_ace_javascript