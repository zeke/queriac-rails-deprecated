en:
  formtastic:
    titles:
      post_details: "Post details"
    labels:
      command:
        name: "Name"
        script: "Script"
        shared: "Share this command with other users"
    placeholders:
      command:
        name: "Google Search"
        keyword: "g"
        script: "alert('foo');"
        description: "Search the web using Google"
    actions:
      create: "Save %{model}"
      update: "Save changes"
      reset: "Reset form"
      cancel: "Cancel and go back"
      dummie: "Launch!"